 {# <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home.index')}}">Signalert</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
      {% if app.user %}
      <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto">        
          <li class="nav-item">
            <a class="nav-link logit" href="{{ path('signalement.index') }}">Signalements</a>
          </li>        
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle logit" data-bs-toggle="dropdown" id="ddown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Gestion</a>
            <div class="dropdown-menu">
              <a class="dropdown-item" href="{{ path('structure.index') }}">Structures</a>
              <a class="dropdown-item" href="{{ path('organisme.index') }}">Organismes</a>
              <a class="dropdown-item" href="{{ path('resistance.index') }}">Résistances</a>
              <a class="dropdown-item" href="{{ path('infection.index') }}">Infections</a>
              <a class="dropdown-item" href="{{ path('etiologie.index') }}">Étiologies</a>
              <a class="dropdown-item" href="{{ path('agent.index') }}">Agents</a>            
            </div>
          </li>
        </ul>
        <div class="nav-item dropdown">
          <a class="nav-link dropdown-toggle logit" data-bs-toggle="dropdown" id="ddown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.prenom}}</a>
            <div class="dropdown-menu  dropdown-menu-end">
              <a class="dropdown-item" href="{{ path('user.edit', {id : app.user.id}) }}">Paramettre</a>
              <a class="dropdown-item" href="{{ path('security.logout') }}">Déconnexion</a> 
            </div>
        </div>
      </div>
      {% endif %}
  </div>
</nav> #}

<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="gridsignalement p-0 mx-4">
    <a class="navbar-brand col1" href="{{ path('home.index')}}">Signalert</a>
    {% if app.user %}
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>      
      {# <div class="collapse navbar-collapse" id="navbarColor01"> #}
        {# <ul class="navbar-nav me-auto">         #}
    <div class="nav-item col1">
      <a class="nav-link logit" href="{{ path('signalement.index') }}">Signalements</a>
    </div>        
    <div class="nav-item dropdown col1">
      <a class="nav-link dropdown-toggle logit" data-bs-toggle="dropdown" id="ddown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Gestion</a>    
      <div class="dropdown-menu">
        <a class="dropdown-item" href="{{ path('structure.index') }}">Structures</a>
        <a class="dropdown-item" href="{{ path('organisme.index') }}">Organismes</a>
        <a class="dropdown-item" href="{{ path('resistance.index') }}">Résistances</a>
        <a class="dropdown-item" href="{{ path('infection.index') }}">Infections</a>
        <a class="dropdown-item" href="{{ path('etiologie.index') }}">Étiologies</a>
        <a class="dropdown-item" href="{{ path('agent.index') }}">Agents</a>            
      </div>
    </div>       
    <div class="nav-item dropdown collast">
      <a class="nav-link dropdown-toggle logit" data-bs-toggle="dropdown" id="ddown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.prenom}}</a>
        <div class="dropdown-menu  dropdown-menu-end">
          <a class="dropdown-item" href="{{ path('user.edit', {id : app.user.id}) }}">Paramettre</a>
          <a class="dropdown-item" href="{{ path('security.logout') }}">Déconnexion</a> 
        </div>
    </div>
      {# </div> #}
      {% endif %}
  </div>
</nav>

