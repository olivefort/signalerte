{% extends 'base.html.twig' %}

{% block title %}Étiologie{% endblock %}

{% block body %}
    <div class='container mt-4'>
        <h1>Ajouter un agent étiologique</h1>

        {{ form_start(form) }}

        <div class="form-group">
            {{ form_label(form.agent) }}      
            {{ form_widget(form.agent) }}
            <div class="form-error">{{ form_errors(form.agent) }}</div>
            {# {{ form_help(form.name) }} #}
        </div>

        <div class="form-group">
            {{ form_row(form.submit) }}      
        </div>
        {{ form_end(form) }}
    <div>


{% endblock %}