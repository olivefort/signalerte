{% extends 'base.html.twig' %}

{% block title %}Infection{% endblock %}

{% block body %}
    <div class='container mt-4'>
        <h1>Modifier une infection</h1>

        {{ form_start(form) }}

        <div class="form-group">
            {{ form_label(form.infection) }}      
            {{ form_widget(form.infection) }}
            <div class="form-error">{{ form_errors(form.infection) }}</div>
            {# {{ form_help(form.name) }} #}
        </div>

        <div class="form-group">
            {{ form_row(form.submit, { 'label': "Modifier l'infection"}) }}      
        </div>
        {{ form_end(form) }}
    <div>


{% endblock %}