{% extends 'base.html.twig' %}

{% block title %}Résistance{% endblock %}

{% block body %}
    <div class='container mt-4'>
        <h1>Modifier une résistance</h1>

        {{ form_start(form) }}

        <div class="form-group">
            {{ form_label(form.type) }}      
            {{ form_widget(form.type) }}
            <div class="form-error">{{ form_errors(form.type) }}</div>
            {# {{ form_help(form.name) }} #}
        </div>

        <div class="form-group">
            {{ form_row(form.submit, { 'label': "Modifier la résistance"}) }}      
        </div>
        {{ form_end(form) }}
    <div>


{% endblock %}